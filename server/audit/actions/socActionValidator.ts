export function validateSOCAction(action: { actionType?: string; confidence?: number; correlationScore?: number }) { if (/block/i.test(action.actionType ?? "")) return { valid: false, reason: "Block cannot be executed automatically" }; if ((action.confidence ?? 0) < 50) return { valid: false, reason: "Confidence below threshold" }; return { valid: true, reason: "Action is advisory-safe" }; }
